# Dependencias para el Comparador de Archivos
# La aplicación usa principalmente bibliotecas estándar de Python

# Bibliotecas estándar incluidas en Python:
# - tkinter (interfaz gráfica)
# - difflib (comparación de archivos)
# - threading (procesamiento en segundo plano)
# - datetime (fechas y horas)
# - pathlib (manejo de rutas)
# - os (operaciones del sistema)

# Dependencia para la funcionalidad de "Arrastrar y Soltar" (Drag & Drop)
tkinterdnd2

# Dependencia opcional para futuras exportaciones a Word
python-docx==0.8.11

# Python 3.6+ requerido

# Para instalar todas las dependencias, ejecuta:
# pip install -r requirements.txt